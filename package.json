{
  "private": true,
  "scripts": {
    "test": "npm run test:loyalty",
    "test:loyalty": "node --test test/loyalty.test.js",
    "test:rules": "firebase emulators:exec --only firestore \"node --test test/firestore.rules.test.mjs\"",
    "check:functions": "node --check functions/ledger.js && node --check functions/loyalty.js && node --check functions/validation.js && node --check functions/referrals.js && node --check functions/notifications.js && node --check functions/rewards.js && node --check functions/index.js",
    "test:all": "npm run check:functions && npm run test:loyalty && npm run test:rules"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^14.17.0"
  }
}
